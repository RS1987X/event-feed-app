########################################
# Example environment variables (.env)
# Copy this file to .env and fill values
# This file is safe to commit; .env is not
########################################

# Telegram Bot (FREE)
# Get from @BotFather after creating a bot
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Optional: store a default chat id for testing
# Obtain using: python3 scripts/telegram_get_chat_id.py
TELEGRAM_CHAT_ID=

# SMTP Email (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_ADDRESS=alerts@yourcompany.com

# Other app settings (examples)
EMAIL_FETCH_LIMIT=10

########################################
# PRESS RELEASE VIEWER CONFIGURATION
########################################

# Option 1: DOCUMENT_BASE_URL (for static content hosting)
# If source_url is missing, alerts will link to ${DOCUMENT_BASE_URL}/{press_release_id}.
# Example: https://news.mycorp.com/pr
DOCUMENT_BASE_URL=

# Option 2: VIEWER_BASE_URL (for Streamlit web viewer - RECOMMENDED)
# This enables:
# - Full press release viewing with formatted content
# - Alert feedback buttons (correct/incorrect)
# - Mobile-friendly iPhone access
# 
# Deployment Options:
#
# A) Cloud Run (RECOMMENDED for production) - ~$0.05/month
#    1. Deploy: bash deploy_viewer.sh
#    2. Set URL: VIEWER_BASE_URL=https://pr-viewer-xyz.run.app
#    3. Enables feedback tracking with alert_id query params
#
# B) Local network (for testing on same WiFi)
#    1. Find your IP: ip addr show | grep "inet 192"
#    2. Start viewer: streamlit run scripts/view_pr_web.py --server.address 0.0.0.0
#    3. Set URL: VIEWER_BASE_URL=http://192.168.1.100:8501
#    4. Access from iPhone on same network
#
# C) Ngrok tunnel (for temporary public access)
#    1. Install: brew install ngrok (Mac) or download from ngrok.com
#    2. Start tunnel: ngrok http 8501
#    3. Start viewer: streamlit run scripts/view_pr_web.py
#    4. Set URL: VIEWER_BASE_URL=https://abc123.ngrok.io (from ngrok output)
#
# D) Localhost (development only - won't work on iPhone)
#    VIEWER_BASE_URL=http://localhost:8501

VIEWER_BASE_URL=http://localhost:8501
